type Puzzle = {
    name: string,
    description: string,
    slug: string,
    createdBy: string,
    complexityScore: number,
    upVotes: number,
    downVotes: number,
};

export default Puzzle;
